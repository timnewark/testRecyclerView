package com.example.testrecyclerview;

public interface RecyclerViewInterface{
    //interface is a contract between any class that implements it and makes sure that class has methods etc.
    // this will change the clicks.
    void onItemClick (int position);
}
